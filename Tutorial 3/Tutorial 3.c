#include <stdio.h>

int main() {
	
/*	//Exercise I
	double r,d,c,a;
	const double PI = 22.0/7.0;
	
	printf("Enter the radius: ");
	scanf("%lf", &r);
	
	d = 2*r;
	c = 2 * PI * r;
	a = PI * r *r;
	
	printf("Diameter = %.2lf\nCircumference = %.2lf\nArea = %.2lf", d,c,a);
	
*/

/*	//Exercise II
	int num;
	printf("Enter a five digit number: ");
	scanf("%d", &num);
	
	printf("%d\t%d\t%d\t%d\t%d", num/ 10000,     (num% 10000) / 1000,     ((num% 10000) % 1000)/100,    (((num% 10000) % 1000)%100)/10,     (((num% 10000) % 1000)%100)%10 );
*/

/*	//Exercise III
	int i = 1, j = 2, k = 3, m = 2;

    printf("%d\n", i == 1);
    printf("%d\n", j == 3);
    printf("%d\n", i >= 1 && j < 4);
    printf("%d\n", k + m < j || 3 - j >= k);
    printf("%d\n", !m);
    printf("%d\n", !(j - m));
*/

/*	//Exercise IV- III
	double mark;
	
	printf("Enter your mark for IP module: ");
	scanf("%lf", &mark);
	
	if(100>= mark && mark>=80)
		printf("Your grade is A");
		
	else if(79>= mark && mark>=70)
		printf("Your grade is B");
		
	else if(69>= mark && mark>=45)
		printf("Your grade is C");
		
	else if(mark < 45)
		printf("Your grade is F");
*/		

	return 0;
}
