#include <stdio.h>

int main(){
/*	//2.II
	double length, width, perimeter, area;
	
	printf("Enter the length: ");
	scanf("%lf", &length);
	
	printf("Enter the width: ");
	scanf("%lf", &width);
	
	perimeter = (length + width) * 2;
	area = length * width;
	
	printf(" Perimeter and Area of the rectangle are: %.0lf and %.0lf", perimeter, area);
*/


/*	//2.III
	float mark1, mark2, average;
	
	printf("Enter Marks 1: ");
	scanf("%f", &mark1);
	
	printf("Enter Marks 2: ");
	scanf("%f", &mark2);
	
	average = (mark1 + mark2) / 2;
	
	printf("%.2f", average);
*/	
	return 0;
}
